{
  "name": "@quaisr/get-blocks-action",
  "version": "0.1.0",
  "description": "GitHub Action to get Blocks within the Quaisr registry",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist",
    "test": "jest",
    "lint": "eslint src/**/*.ts",
    "format": "prettier --write **/*.ts"
  },
  "keywords": [
    "github",
    "actions",
    "quaisr",
    "register",
    "block"
  ],
  "author": "Quaisr",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
